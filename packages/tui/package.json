{
  "name": "@auto-guard/tui",
  "version": "0.3.0",
  "description": "Full-screen interactive management console (Guard TUI) for auto-guard.",
  "license": "MIT",
  "type": "module",
  "main": "src/tui.ts",
  "bin": {
    "auto-guard-tui": "src/tui.ts"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "dependencies": {
    "@auto-guard/cli": "workspace:*",
    "@auto-guard/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
