{
  "name": "@auto-guard/host-pi",
  "version": "0.3.0",
  "description": "Pi Coding Agent adapter for auto-guard.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "node": ">=22.18.0"
  },
  "dependencies": {
    "@auto-guard/core": "workspace:*",
    "@auto-guard/host-runtime": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
