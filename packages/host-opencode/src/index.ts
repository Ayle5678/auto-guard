/**
 * Package entry: re-exports the plugin so OpenCode can load this package
 * either as a directory (dist/ → index.js) or as a single file (dist/plugin.js).
 */
export { AutoGuard } from './plugin.ts'
