{
  "name": "@auto-guard/core",
  "version": "0.3.0",
  "description": "Zero-host-dependency decision engine for the auto-guard command review guard.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "node": ">=22.18.0"
  },
  "optionalDependencies": {
    "better-sqlite3-multiple-ciphers": "^13.0.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
