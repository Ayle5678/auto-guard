# 02 — 询问四选项值匹配重构

**What to build:** Pi 会话里 LLM 不确定时的四选项对话框（同意/本会话都同意/拒绝/本会话都拒绝）在英文设置下显示英文标签，用户的选择语义（是否写会话记忆、拒绝理由透传）与重构前完全一致。实现上把「文案即匹配键」解除：选项结构化为值（四态）+ 双语标签，core 只认值，Pi 用两语言标签反查表承接 UI 返回的标签字符串；注意 Pi 适配层有**两处**按中文字面匹配——`resolveAskMemory` 调用处，以及据此决定是否追问拒绝原因的分支判断——两处都必须改为值判断，无法解析的输入仍然 block。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 值矩阵测试：四个值 × 两语言标签全部解析正确；垃圾输入 block
- [ ] Pi 交互测试（fake UI）断言英文设置下标签为英文、追问拒绝原因分支照常触发、会话记忆写入语义不变
- [ ] allow/deny 与 reason 透传行为与重构前一致
