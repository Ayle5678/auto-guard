# 04 — 帮助页重排：全宽折行 + 可滚全文

**What to build:** 帮助页从"46 行无条件输出、超屏静默切尾"改为感知视口：键位表与命令映射的长行按帧宽折行（命令表不再卡 38% 右栏预算），整页纳入滚动体系（`PgUp`/`PgDn`/`g`/`G`，与输出面板同款），24 行终端也能到达最底部的命令模式提示行。键位表与实际键位 100% 一致（含 02 的滚动键）。

**Blocked by:** 02 — 回执 argv 与滚动键（键位文档必须与已落地的键位一致）。

**Status:** ready-for-agent

- [x] 无头断言：80×24 视口下帮助页默认显示首屏，`g` 滚顶（顶部）后可翻页到达 `: <command>` 尾行（内容不删减）
- [x] 无头断言：长命令签名（`init […]`）在 80 列下折行完整可达、无字符丢失
- [x] 键位表含滚动键；与实际路由一致（帮助屏不响应的键不出现在表中）
- [x] 双语（zh/en）与 `NO_COLOR` 渲染不破；`pnpm -r typecheck` / `pnpm -r test` / `pnpm smoke` 全绿
