# 03 — init 规则更新的交互预览与双语文案（cli）

**What to build:** 交互式 `auto-guard init` 在检测到本地缺失的出厂模式时，逐条展示将追加的规则（pattern + 理由）的 diff 预览，用户确认后执行 02 的追加流程、拒绝则保持原样且不再纠缠；预览/确认/跳过/已最新提示全部进消息目录，中英键类型强制对齐，跟随语言设置（ADR-0011 机制）。

**Blocked by:** 02 — 检测/追加/备份操作层就绪后才有可预览的对象。

**Status:** ready-for-agent

- [ ] 交互运行：缺失条目以"将追加什么"的可读清单展示，确认后落盘结果与非交互路径完全一致
- [ ] 拒绝后本次安装不再追问，文件不变
- [ ] 新增文案全部走消息目录，中英键一一对应，语言设置四层解析生效
- [ ] 非交互 flag 与交互路径共用同一操作层，行为一致性有测试钉住
